const initialState = [
    {
        status: 'ready'
    },
    {
        status: 'ready'
    },
    {
        status: 'ready'
    },
    {
        status: 'ready'
    }
]

const pomodoroReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default pomodoroReducer
